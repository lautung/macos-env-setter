# macOS 环境变量配置工具（macos-env-setter）

一个 SwiftUI 原生 macOS 窗口应用，让用户在一个全局列表里管理环境变量，并同时写入 shell 配置与 GUI 应用两个互不相通的层。

## Language

### 记录与取值

**变量记录**:
一条被工具管理的环境变量。包含 key、原始值、作用层开关（shell/GUI 各一）和来源标记。
_Avoid_: 条目、项、env entry

**原始值**:
变量记录中保留 `$` 引用与 `${…}` 展开原文的 value 文本。工具存储和写回时都保持原样，不预展开。
_Avoid_: 字面值（指展开后的值）、展开值

**作用层**:
变量生效的两个互不相通的目标——shell 层（配置文件，对终端生效）与 GUI 层（launchd 域，对 Dock/Finder 启动的应用生效）。每条变量各自独立开关。
_Avoid_: 作用域、范围

**来源**:
变量记录的出身标记——「工具创建」或「导入」（收编自用户手写配置）。

**秘密值**:
变量记录上的展示标记：列表与预览默认打码，编辑表单可临时显示明文。只影响展示，不改变写入两层的任何内容——因此改它不算「待生效」，而是立刻单独存进本地状态。
_Avoid_: 加密值、隐藏值、密文

**待生效**:
内存里的编辑与磁盘上的已应用状态不一致的状态。界面上以橙色时钟标识贯穿列表、详情与工具栏的「应用 (N)」按钮；点「应用」才一次性落盘。
_Avoid_: 未保存、脏状态、pending

### 写入与文件

**标记块**:
`~/.zprofile` 中由标记注释围起、工具独占读写的区域。块外内容工具永不触碰。
_Avoid_: 管理区、托管块

**逐字保留行**:
标记块内无法解析为简单 `export` 的内容（复杂行、注释、空行）。工具原样写回、不重写、不管理，界面上只读展示。
_Avoid_: 原样行、verbatim 行

**GUI 层脚本**:
工具生成、由 LaunchAgent 在每次登录时执行的 `setenv.sh`（`~/Library/Application Support/EnvSetter/`）。
按声明顺序先做 shell 变量赋值、再逐条 `launchctl setenv`，使 `$` 引用在 GUI 层也照常展开；`setenv.sh --print` 只打印将要注入的 `KEY=VALUE`，供回读比对与诊断用。
_Avoid_: 启动脚本、env 脚本

**显式应用**:
将内存中的编辑一次性写入两层（先备份）的动作。编辑本身只改内存，直到应用才落盘。
_Avoid_: 保存、同步

**收编**:
把标记块外用户手写的简单 `export` 行接管为变量记录的动作；原行被注释掉以保留手工回退路径，PATH 行合并进 PATH 专用列表。
_Avoid_: 导入文件、迁移

**漂移**:
标记块的实际内容与工具最后写入的快照不一致——意味着用户手工改过块内内容。处理原则：以文件为准重新载入并提示。
_Avoid_: 冲突、脏状态

**基线备份**:
首次收编/接管配置文件时创建的、永不随清理轮转删除的备份。

**PATH 锚点**:
PATH 有序列表中代表「继承既有 PATH」的特殊条目（终端层为启动时的 `$PATH`；GUI 层为 agent 启动时继承到的 PATH，登录时即 launchd 的默认 PATH）。用户条目排在锚点之前即为前插。
_Avoid_: 系统路径、默认路径
